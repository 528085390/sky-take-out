<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">


    <select id="getByUserIdAndDishId" resultType="com.sky.entity.ShoppingCart">
            SELECT * FROM shopping_cart WHERE user_id = #{userId} AND dish_id = #{shoppingCartDTO.dishId}
            <if test="shoppingCartDTO.dishFlavor != null">
                AND dish_flavor = #{shoppingCartDTO.dishFlavor}
            </if>
    </select>
</mapper>
