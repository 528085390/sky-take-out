<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">

    <select id="list" resultType="com.sky.entity.ShoppingCart">
        SELECT * FROM shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
        </where>
    </select>


    <insert id="insertBatch">
        INSERT INTO shopping_cart(
            name, image, dish_id, setmeal_id, dish_flavor, number, amount,
            create_time, user_id)
        VALUES
        <foreach collection="shoppingCarts" item="dish" separator=",">(
            #{dish.name}, #{dish.image}, #{dish.dishId}, #{dish.setmealId}, #{dish.dishFlavor},
            #{dish.number},#{dish.amount}, #{dish.createTime}, #{dish.userId})
        </foreach>

    </insert>
</mapper>
